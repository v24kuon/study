
<% if post.favorited_by?(current_user) %>
  <%=link_to post.favorites.count, post_favorites_path(post.id), method: :delete, remote: true %> いいね
<% else %>
  <%=link_to post.favorites.count, post_favorites_path(post.id), method: :post, remote: true %> いいね
<% end %>
